/** Geometry is checked by the spatial extractor before any field is mapped. */
export function extractTicSpatialValueLines(blocks: unknown, suppliedWidth?: number, suppliedHeight?: number): string[];
