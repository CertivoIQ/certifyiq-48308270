import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync, existsSync } from "node:fs";
import { routePha50058Transaction } from "../src/lib/pha-50058-transaction-router.mjs";

const read = (path) => readFileSync(new URL(`../${path}`, import.meta.url), "utf8");

const migration = read("supabase/migrations/20260828012000_workspace_profiles_and_program_applicability.sql");
const hotmaCorrection = read("supabase/migrations/20260828022000_split_hotma_section_overlays.sql");
const phaRoutingMigration = read("supabase/migrations/20260828030000_pha_hotma_cohort_profile.sql");
const transactionMigration = read("supabase/migrations/20260828031000_pha_50058_transaction_queue.sql");
const submissionMigration = read("supabase/migrations/20260828220000_pha_50058_submission_history.sql");
const controlVisibilityMigration = read("supabase/migrations/20260828210000_pha_control_member_visibility.sql");
const configurator = read("src/components/workspace-profile-configurator.tsx");
const phaDashboard = read("src/components/pha-dashboard.tsx");
const phaHotma = read("src/components/pha-hotma-readiness-workspace.tsx");
const phaHotmaRoute = read("src/routes/_authenticated/pha-hotma.tsx");
const pha50058Workspace = read("src/components/pha-50058-submission-workspace.tsx");
const pha50058Route = read("src/routes/_authenticated/pha-50058.tsx");
const phaModulePage = read("src/components/pha-module-page.tsx");
const dashboardRoute = read("src/routes/_authenticated/dashboard.tsx");
const appShell = read("src/components/app-shell.tsx");

test("workspace profile stores organization type and multiple programs", () => { assert.match(migration, /customer_workspace_profiles/); assert.match(migration, /selected_programs text\[\]/); assert.match(migration, /pha_programs text\[\]/); assert.match(configurator, /Select all that apply/); });
test("HOTMA is derived instead of offered as a selectable program", () => { assert.match(hotmaCorrection, /derive_workspace_overlays/); assert.match(hotmaCorrection, /hotma_102/); assert.match(hotmaCorrection, /hotma_103/); assert.match(hotmaCorrection, /hotma_104/); assert.doesNotMatch(configurator, /\["hotma"\s*,/i); assert.match(configurator, /HOTMA is not a selectable program/); });
test("multifamily HOTMA section 104 is limited to PBRA and Section 202\/8", () => { assert.match(hotmaCorrection, /programs && array\['section8_pbra','section202_8'\]::text\[]/); assert.match(hotmaCorrection, /'section202_811_prac','section811_pra','section236_irp','sprac'/); const section104Case = hotmaCorrection.match(/case when programs && array\['section8_pbra','section202_8'\][\s\S]*?then 'hotma_104' end/); assert.ok(section104Case); assert.doesNotMatch(section104Case[0], /section202_811_prac|section811_pra|section236_irp|sprac/); });
test("PHA workspace requires cohort and HUD-50058 reporting path for HOTMA routing", () => { assert.match(phaRoutingMigration, /pha_hotma_cohort/); assert.match(phaRoutingMigration, /NON_MTW_NON_FRS/); assert.match(phaRoutingMigration, /INITIAL_MTW/); assert.match(phaRoutingMigration, /MTW_EXPANSION/); assert.match(phaRoutingMigration, /FRS_EXCLUSIVE/); assert.match(phaRoutingMigration, /hud_50058_reporting_path/); assert.match(configurator, /PHA cohort and HUD-50058 reporting path are required/); });
test("PHA command center routes implementation timing through the deterministic HOTMA engine", () => { assert.match(phaDashboard, /classifyPhaHotmaImplementation/); assert.match(phaDashboard, /PHA-HOTMA-FULL-SECTIONS-102-104/); assert.match(phaDashboard, /PHA_HOTMA_DEADLINE_PENDING_HUD_GUIDANCE/); assert.match(phaDashboard, /transaction_effective_date:"2027-01-01"/); });
test("PHA HUD-50058 transaction queue is tenant isolated and routed deterministically", () => { assert.match(transactionMigration, /pha_50058_transactions/); assert.match(transactionMigration, /using \(user_id = auth\.uid\(\)\)/); assert.match(phaModulePage, /routePha50058Transaction/); assert.match(phaModulePage, /awaiting HUD guidance/i); });
const validatedTransaction = { program: "hcv", transaction_type: "annual_reexamination", pha_hotma_cohort: "NON_MTW_NON_FRS", hud_50058_reporting_path: "HUD_50058_2024", program_applicability_validated: true, controlled_source_release_approved: true, current_rule_version_validated: true, full_hotma_policy_set_validated: true, reporting_path_validated: true, software_compatibility_validated: true };
test("non-MTW transaction before full implementation is classified pre-implementation", () => { assert.equal(routePha50058Transaction({ ...validatedTransaction, effective_date: "2026-12-01" }).status, "PRE_IMPLEMENTATION"); });
test("non-MTW transaction on the full-compliance date is ready when controls are validated", () => { assert.equal(routePha50058Transaction({ ...validatedTransaction, effective_date: "2027-01-01" }).status, "READY"); });
test("MTW transaction does not inherit the non-MTW January 2027 enforcement date", () => { const result = routePha50058Transaction({ ...validatedTransaction, pha_hotma_cohort: "INITIAL_MTW", effective_date: "2027-01-01" }); assert.equal(result.status, "AWAITING_HUD_GUIDANCE"); assert.equal(result.reason_code, "PHA_HOTMA_DEADLINE_PENDING_HUD_GUIDANCE"); });
test("transaction routing blocks when required operational controls are unresolved", () => { const result = routePha50058Transaction({ ...validatedTransaction, full_hotma_policy_set_validated: false, effective_date: "2027-01-01" }); assert.equal(result.status, "BLOCKED"); assert.ok(result.missing_inputs?.includes("full_hotma_policy_set_validated")); });
test("program applicability supports property, building, and unit scope with tenant isolation", () => { assert.match(migration, /property_program_applicability/); assert.match(migration, /coverage_level in \('property', 'building', 'unit'\)/); assert.match(migration, /building_id text/); assert.match(migration, /unit_id text/); assert.match(migration, /user_id uuid not null default auth\.uid\(\)/); assert.match(migration, /Users manage own property program applicability/); assert.doesNotMatch(migration, /using \(true\) with check \(true\)/); });
test("PHA accounts route to an agency-specific command center and modules", () => { assert.match(dashboardRoute, /organization_type === "pha"/); assert.match(dashboardRoute, /<PhaDashboard/); for (const route of ["pha-families", "pha-50058", "pha-inspections"]) { assert.ok(existsSync(new URL(`../src/routes/_authenticated/${route}.tsx`, import.meta.url))); assert.match(appShell, new RegExp(route)); } assert.ok(existsSync(new URL("../src/routes/_authenticated/pha-hotma.tsx", import.meta.url))); assert.match(appShell, /hotma-readiness/); });
test("multifamily navigation exposes organization and program configuration", () => { assert.match(appShell, /workspace-setup/); assert.match(appShell, /Multifamily workspace/); assert.match(appShell, /PHA workspace/); });
test("PHA command center changes title, actions, and live metrics by agency role",()=>{ assert.match(phaDashboard,/Executive Command Center/); assert.match(phaDashboard,/HCV \/ PBV Operations/); assert.match(phaDashboard,/Public Housing Operations/); assert.match(phaDashboard,/Inspection Operations/); assert.match(phaDashboard,/phaRole/); assert.match(phaDashboard,/pha_reasonable_accommodation_requests/); assert.match(phaDashboard,/pha_waiting_list_applicants/); assert.match(phaDashboard,/pha_portability_cases/); assert.match(phaDashboard,/role-filtered/); });
test("PHA HOTMA route is a live readiness workspace instead of the generic placeholder",()=>{ assert.match(phaHotmaRoute,/PhaHotmaReadinessWorkspace/); assert.doesNotMatch(phaHotmaRoute,/PhaModulePage/); assert.match(phaHotma,/pha_authoritative_control_state/); assert.match(phaHotma,/pha_notice_policy_overlays/); assert.match(phaHotma,/pha_50058_transactions/); assert.match(phaHotma,/source_release_status/); assert.match(phaHotma,/rule_version_status/); assert.match(phaHotma,/software_compatibility_status/); assert.match(phaHotma,/January 1, 2027/); assert.match(phaHotma,/HUD guidance pending/); });
test("authorized PHA members can read but not mutate agency authoritative controls",()=>{ assert.match(controlVisibilityMigration,/PHA users read agency authoritative controls/); assert.match(controlVisibilityMigration,/pha_workspace_memberships/); assert.match(controlVisibilityMigration,/m\.active = true/); assert.doesNotMatch(controlVisibilityMigration,/for all to authenticated/); });
test("HUD-50058 database routing status is derived from cohort and authoritative controls",()=>{ assert.match(submissionMigration,/derive_pha_50058_routing_status/); assert.match(submissionMigration,/AWAITING_HUD_GUIDANCE/); assert.match(submissionMigration,/PRE_IMPLEMENTATION/); assert.match(submissionMigration,/date '2027-01-01'/); assert.match(submissionMigration,/full_hotma_policy_set_validated/); assert.match(submissionMigration,/software_compatibility_validated/); });
test("HUD-50058 submission never assumes an API transport and preserves immutable history",()=>{ assert.match(submissionMigration,/pha_50058_transport_profiles/); assert.match(submissionMigration,/Validated HUD-50058 API transport is not configured/); assert.match(submissionMigration,/pha_50058_submission_attempts/); assert.match(submissionMigration,/pha_50058_submission_events/); assert.match(submissionMigration,/External submission reference is required when recording transmission/); assert.match(submissionMigration,/Final HUD-50058 submission state is immutable/); assert.match(submissionMigration,/HUD-50058 response requires a transmitted submission/); });
test("HUD-50058 route exposes preparation, transmission, response and attempt history",()=>{ assert.match(pha50058Route,/Pha50058SubmissionWorkspace/); assert.doesNotMatch(pha50058Route,/PhaModulePage/); assert.match(pha50058Workspace,/Prepare external submission/); assert.match(pha50058Workspace,/Record transmitted/); assert.match(pha50058Workspace,/Record accepted/); assert.match(pha50058Workspace,/Record rejected/); assert.match(pha50058Workspace,/Submission attempt history/); assert.match(pha50058Workspace,/does not claim a HUD transmission occurred/); });
